# BEGET_CATALOG.md — Каталог Beget для курса

Это справочный документ для агента-архитектора. Здесь актуальные тарифы Beget на момент составления курса и рекомендации, что брать под какой тип проекта.

> **Принцип курса:** один VPS на всё (приложение + БД + nginx). Никаких отдельных серверов под БД и воркеры. Это нормально для MVP до тысяч пользователей.

---

## VPS (Virtual Private Server)

Это основной выбор для курса. Покупаешь один VPS, на нём всё живёт.

| Конфигурация | Цена/день | + IPv4 | Цена/мес | Под что |
|---|---|---|---|---|
| 1 CPU / 1 ГБ RAM / 10 ГБ NVMe | 11 ₽ | +5 ₽ | ~480 ₽ | Очень простой бот, тестовый стенд |
| **1 CPU / 2 ГБ RAM / 15 ГБ NVMe** | **17 ₽** | +5 ₽ | **~660 ₽** | **Telegram-бот** |
| **2 CPU / 4 ГБ RAM / 40 ГБ NVMe** | **33 ₽** | +5 ₽ | **~1140 ₽** | **Веб-приложение MVP, малая нагрузка** |
| 4 CPU / 6 ГБ RAM / 80 ГБ NVMe | 68 ₽ | +5 ₽ | ~2190 ₽ | Веб с реальными чатами, активный MVP |
| 8 CPU / 16 ГБ RAM / 220 ГБ NVMe | 183 ₽ | +5 ₽ | ~5640 ₽ | Серьёзный проект с нагрузкой |

**ОС:** всегда **Ubuntu 24.04 LTS** (поддержка до 2029). Не бери предустановленные образы «с Node.js» или «с Bitrix» — нам нужна чистая база, дальше Claude Code поставит всё сам.

**ДЦ:** Россия (Санкт-Петербург, Москва), Казахстан (Астана), Европа (Рига). Для российских пользователей — РФ.

---

## База данных

В курсе — **PostgreSQL на том же VPS** (ставится через `apt install postgresql`). Бесплатно (входит в стоимость VPS), управляешь сам.

**Beget DBaaS** (managed Postgres) — это бонус-урок для продвинутых, не для базового курса.

---

## S3-совместимое хранилище (для файлов)

| Параметр | Значение |
|---|---|
| Цена | **0,07 ₽/ГБ/день** (~2,1 ₽/ГБ/мес) |
| Минимум | 20 ГБ = **~60 ₽/мес** |
| Запросы | Бесплатно |
| Endpoint | `https://s3.ru1.storage.beget.cloud` |
| Регион | `ru1` |
| Совместимость | AWS SDK / boto3 / aws-cli / rclone |

**Берём всегда**, когда в проекте есть файлы (картинки, документы, аватары). Стоит копейки.

---

## Домены

| Зона | Цена/год |
|---|---|
| **.ru** | **199 ₽** |
| .рф | 199 ₽ |
| .com | 1490 ₽ |
| .net | 1690 ₽ |

NS Beget: `ns1.beget.com`, `ns2.beget.com`. При регистрации в Beget — настраиваются автоматически.

---

## Что не берём в базовом курсе

- ❌ **Виртуальный хостинг** (Blog/Start/Noble/Great) — для статических сайтов и WordPress. Для Telegram-ботов и серьёзных приложений не подходит.
- ❌ **Beget DBaaS** (managed Postgres) — на старте избыточно, PostgreSQL ставим на тот же VPS.
- ❌ **Beget Cloud DB MySQL** — используем PostgreSQL.
- ❌ **CDN, Kubernetes** — не нужно для MVP.

---

## Рекомендации по типу проекта

### Telegram-бот (TrueAI, помощник, агрегатор)

- **VPS:** 1 CPU / 2 ГБ / 15 ГБ — **~660 ₽/мес**
- **БД:** PostgreSQL на том же VPS
- **S3:** если бот работает с файлами (картинки, голосовые) — да, ~60 ₽/мес
- **Домен:** не обязателен (webhook можно без него, на IP)
- **Стек:** Python 3.12 + aiogram 3 или Node.js + grammy

### Веб-приложение MVP без реальных чатов

- **VPS:** 2 CPU / 4 ГБ / 40 ГБ — **~1140 ₽/мес**
- **БД:** PostgreSQL на том же VPS
- **S3:** ~60 ₽/мес
- **Домен:** .ru за 199 ₽/год
- **Стек:** Next.js 15 + TypeScript + PostgreSQL, монолит

### Веб-приложение с реальными чатами (Swois)

- **VPS:** 4 CPU / 6 ГБ / 80 ГБ — **~2190 ₽/мес** (WebSocket нагрузка)
- **БД:** PostgreSQL на том же VPS, Redis для онлайн-пользователей
- **S3:** для аватарок и файлов в чатах, ~60 ₽/мес
- **Домен:** .ru
- **Стек:** Next.js + Socket.IO или Express + ws

### Простой лендинг / сайт-визитка

- **VPS:** 1 CPU / 2 ГБ / 15 ГБ — **~660 ₽/мес** (избыточно, но универсально)
- **БД:** не нужна
- **S3:** не нужно
- **Домен:** .ru
- **Стек:** Astro 5 или Next.js со статическим экспортом

### AI-агрегатор (TrueAI веб-версия)

- **VPS:** 2 CPU / 4 ГБ / 40 ГБ — **~1140 ₽/мес**
- **БД:** PostgreSQL + Redis для очереди задач (генерация — долгая)
- **S3:** обязательно для результатов (картинки/видео)
- **Стек:** Next.js + BullMQ или FastAPI + Celery
- **Особенность:** API нейросетей из РФ — через прокси-провайдер (proxyapi.ru, vsegpt.ru) или VPN на сервере

---

## Минимальная стоимость старта

Базовый комплект для курса:

| Что | Цена |
|---|---|
| VPS 2 CPU / 4 ГБ (универсальный) | 1140 ₽/мес |
| IPv4 | 150 ₽/мес |
| S3 (20 ГБ минимум) | 60 ₽/мес |
| Домен .ru | 199 ₽/год (~17 ₽/мес) |
| **ИТОГО** | **~1367 ₽/мес** |

Для бота без чатов и без файлов — **~660 ₽/мес** (VPS меньше + без S3 + без домена).

---

## Подводные камни Beget

- ✅ Платёж посуточный — можешь сначала купить на день за 17-33 ₽, протестировать.
- ✅ 30 дней триал на shared (но shared нам не подходит).
- ⚠️ Публичный IPv4 на VPS оплачивается **отдельно** (5 ₽/день).
- ⚠️ Поддержка в последнее время отвечает шаблонно. Серьёзные вопросы решает, но не быстро.
- ❌ Для Telegram-ботов и Docker — shared не подходит, только VPS.

---

## Полезные ссылки

- Панель управления: https://cp.beget.com
- Документация API: https://developer.beget.com
- Поддержка: тикеты в панели или https://t.me/beget_support_bot
- Базовые тарифы: https://beget.com/ru/vps
- DBaaS PostgreSQL: https://beget.com/ru/cloud/dbaas-postgresql
- S3 Object Storage: https://beget.com/ru/cloud/storage
